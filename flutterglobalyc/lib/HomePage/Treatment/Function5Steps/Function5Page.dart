import 'package:flutter/material.dart';

class Function5Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(
        child: Text('edu Page'),
      ),
    );
  }
}