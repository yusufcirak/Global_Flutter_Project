import 'package:flutter/material.dart';

class Function2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(
         child: Text('Function2 Page'),
      ),
    );
  }
}